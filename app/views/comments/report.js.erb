// モーダルを非表示
$("#modal-overlay").fadeOut(200);
$(".modal.final-action.report").fadeOut(200);

// フラッシュメッセージを表示
$('#flash-container').html('<div class="flash-message notice">コメントを通報しました</div>');
if (typeof window.fadeOutFlash === 'function') {
  window.fadeOutFlash();
}